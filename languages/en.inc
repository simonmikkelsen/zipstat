<?php

/**
 * Returns an array with all the localizaion mappings.
 * 
 * @public
 * @version 0.0.1
 * @since 0.0.1
 * @return String[] an array with all the localizaion mappings.
 */
function getLocals()
{
	//The stat. site.
	$locale['statSiteFor'] = 'Stats for %1$s'; //%1$s is the title of the site.
	
	return $locale;
}


?>